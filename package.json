{
  "name": "sbke-monorepo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm run start",
    "install:all": "npm install && cd frontend && npm install",
    "data:process": "cd datasets && python nasa_scraper.py && python mission_analyzer.py"
  },
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
